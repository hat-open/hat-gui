$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "hat-gui://events.yaml"
definitions:
    events:
        clients:
            type: array
            items:
                type: object
                required:
                    - remote
                    - user
                properties:
                    remote:
                        type: string
                    user:
                        type: string
